<Grid x:Class="skeleton.more_controls.main_panel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:skeleton.more_controls" 
      mc:Ignorable="d" 
      Margin="5"
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="auto"/>
        <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition  Height="auto"/>
        <RowDefinition/>
    </Grid.RowDefinitions>

        <StackPanel  x:Name="h_stack"  Grid.Row="0" Margin="2" Orientation="Horizontal" Grid.Column="1">
            <local:icon/>
            <local:icon/>
            <local:icon/>
        </StackPanel>

    <StackPanel x:Name="v_stack" Grid.Row="1"  Margin="2">
        <local:icon/>
        <local:icon/>
        <local:icon/>
    </StackPanel>

    <Border x:Name="stage" BorderThickness="2" BorderBrush="LightBlue" Grid.Row="1" CornerRadius="3" Padding="5"  Grid.Column="1"/>
</Grid>
